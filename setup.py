#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='SeeWhence',
      version='0.0.1',
      description='This package is for sequence classification.',
      author='Ben Burtenshaw',
      author_email='ben.burtenshaw@gmail.com',
#       packages=find_packages(exclude=["*.tests", "*.tests.*", "tests.*", "tests"]),
#       license='LICENSE.txt',
    )
